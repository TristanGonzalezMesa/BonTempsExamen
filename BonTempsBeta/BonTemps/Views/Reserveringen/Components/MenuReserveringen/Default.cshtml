@model IEnumerable<BonTemps.Models.ReserveringMenu>

<h4>Menus</h4>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Menu.Naam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Menu.Prijs)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Menu.Naam)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Menu.Prijs)
                </td>
            </tr>
        }
    </tbody>
</table>