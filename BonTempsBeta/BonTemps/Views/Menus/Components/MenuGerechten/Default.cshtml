@model IEnumerable<BonTemps.Models.MenuGerecht>

<h4>Gerechten</h4>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Gerecht.Naam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Gerecht.GerechtSoortId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Gerecht.Naam)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Gerecht.GerechtSoortId)
                </td>
            </tr>
        }
    </tbody>
</table>